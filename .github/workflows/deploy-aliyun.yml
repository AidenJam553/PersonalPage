name: Deploy to Aliyun OSS

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci || npm install
      
      - name: Build project
        run: |
          echo "Building project..."
          ls -la node_modules/.bin/ || echo "node_modules/.bin not found"
          npx vite build
          echo "✅ Build completed"
      
      - name: Setup Python for OSS SDK
        run: |
          python3 --version
          pip3 install oss2
      
      - name: Deploy to OSS using Python SDK
        env:
          OSS_ENDPOINT: ${{ secrets.ALIYUN_OSS_ENDPOINT }}
          OSS_ACCESS_KEY_ID: ${{ secrets.ALIYUN_ACCESS_KEY_ID }}
          OSS_ACCESS_KEY_SECRET: ${{ secrets.ALIYUN_ACCESS_KEY_SECRET }}
          OSS_BUCKET: ${{ secrets.ALIYUN_OSS_BUCKET }}
        run: |
          python3 << 'EOF'
          import os
          import oss2
          from pathlib import Path
          import mimetypes
          
          endpoint = os.environ['OSS_ENDPOINT']
          access_key_id = os.environ['OSS_ACCESS_KEY_ID']
          access_key_secret = os.environ['OSS_ACCESS_KEY_SECRET']
          bucket_name = os.environ['OSS_BUCKET']
          
          print(f"Connecting to OSS: {bucket_name} at {endpoint}")
          auth = oss2.Auth(access_key_id, access_key_secret)
          bucket = oss2.Bucket(auth, endpoint, bucket_name)
          
          # Check bucket ACL (permissions)
          try:
              acl = bucket.get_bucket_acl().acl
              print(f"📋 Bucket ACL: {acl}")
              if acl != oss2.BUCKET_ACL_PUBLIC_READ:
                  print("⚠️ Warning: Bucket is not set to PUBLIC_READ!")
                  print("   Please set bucket ACL to '公共读' in OSS console:")
                  print("   Permission Control → Read/Write Permissions → Public Read")
          except Exception as e:
              print(f"⚠️ Could not check bucket ACL: {e}")
          
          dist_path = Path('dist')
          if not dist_path.exists():
              print("❌ Error: dist directory not found!")
              exit(1)
          
          # MIME type mapping
          mime_types = {
              '.html': 'text/html; charset=utf-8',
              '.js': 'application/javascript; charset=utf-8',
              '.css': 'text/css; charset=utf-8',
              '.json': 'application/json',
              '.png': 'image/png',
              '.jpg': 'image/jpeg',
              '.jpeg': 'image/jpeg',
              '.gif': 'image/gif',
              '.svg': 'image/svg+xml',
              '.ico': 'image/x-icon',
              '.woff': 'font/woff',
              '.woff2': 'font/woff2',
              '.ttf': 'font/ttf',
              '.eot': 'application/vnd.ms-fontobject'
          }
          
          def get_content_type(file_path):
              suffix = file_path.suffix.lower()
              return mime_types.get(suffix, mimetypes.guess_type(str(file_path))[0] or 'application/octet-stream')
          
          print("\n📤 Uploading files...")
          uploaded_files = []
          for file_path in dist_path.rglob('*'):
              if file_path.is_file():
                  object_name = file_path.relative_to(dist_path).as_posix()
                  object_name = object_name.replace('\\', '/')
                  content_type = get_content_type(file_path)
                  
                  headers = {'Content-Type': content_type}
                  if file_path.suffix.lower() == '.html':
                      headers['Cache-Control'] = 'no-cache'
                      headers['Content-Disposition'] = 'inline'  # Important: display inline, not download
                  else:
                      headers['Cache-Control'] = 'public, max-age=31536000'
                  
                  print(f"  📄 {object_name} → Content-Type: {content_type}")
                  bucket.put_object_from_file(
                      object_name,
                      str(file_path),
                      headers=headers
                  )
                  uploaded_files.append(object_name)
          
          # Configure static website hosting
          print("\n🌐 Configuring static website hosting...")
          try:
              website_config = oss2.models.BucketWebsite(
                  index_file='index.html',
                  error_file='index.html'
              )
              bucket.put_bucket_website(website_config)
              print("✅ Static website hosting configured!")
              
              # Verify configuration
              try:
                  website_info = bucket.get_bucket_website()
                  print(f"   Index file: {website_info.index_file}")
                  print(f"   Error file: {website_info.error_file}")
              except:
                  pass
          except Exception as e:
              print(f"⚠️ Warning: Could not configure static website hosting: {e}")
              print("   Please configure manually in OSS console:")
              print("   Data Management → Static Page → Enable → Set index.html")
          
          # Verify index.html was uploaded
          print("\n🔍 Verifying deployment...")
          try:
              if bucket.object_exists('index.html'):
                  meta = bucket.head_object('index.html')
                  print(f"✅ index.html exists")
                  print(f"   Content-Type: {meta.headers.get('Content-Type', 'Not set')}")
                  print(f"   Content-Disposition: {meta.headers.get('Content-Disposition', 'Not set')}")
              else:
                  print("❌ ERROR: index.html not found in bucket!")
          except Exception as e:
              print(f"⚠️ Could not verify index.html: {e}")
          
          # Print access URLs
          print("\n🌐 Access your website:")
          print(f"   HTTP:  http://{bucket_name}.{endpoint}")
          print(f"   (Use HTTP, not HTTPS for static website hosting)")
          print(f"\n✅ Deployment completed successfully!")
          EOF
      
      - name: Success message
        run: |
          echo "🚀 Website deployed successfully to Aliyun OSS!"
          echo "🌐 Access your site at: http://${{ secrets.ALIYUN_OSS_BUCKET }}.${{ secrets.ALIYUN_OSS_ENDPOINT }}"
          echo "⚠️  Important: Use HTTP (not HTTPS) for OSS static website hosting!"
          echo "⚠️  Do NOT add /index.html at the end!"

