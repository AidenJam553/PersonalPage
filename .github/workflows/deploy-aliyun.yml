name: Deploy to Aliyun OSS

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci || npm install
      
      - name: Build project
        run: |
          echo "Building project..."
          ls -la node_modules/.bin/ || echo "node_modules/.bin not found"
          npx vite build
          echo "✅ Build completed"
      
      - name: Setup Python for OSS SDK
        run: |
          python3 --version
          pip3 install oss2
      
      - name: Deploy to OSS using Python SDK
        env:
          OSS_ENDPOINT: ${{ secrets.ALIYUN_OSS_ENDPOINT }}
          OSS_ACCESS_KEY_ID: ${{ secrets.ALIYUN_ACCESS_KEY_ID }}
          OSS_ACCESS_KEY_SECRET: ${{ secrets.ALIYUN_ACCESS_KEY_SECRET }}
          OSS_BUCKET: ${{ secrets.ALIYUN_OSS_BUCKET }}
        run: |
          python3 << 'EOF'
          import os
          import oss2
          from pathlib import Path
          
          endpoint = os.environ['OSS_ENDPOINT']
          access_key_id = os.environ['OSS_ACCESS_KEY_ID']
          access_key_secret = os.environ['OSS_ACCESS_KEY_SECRET']
          bucket_name = os.environ['OSS_BUCKET']
          
          print(f"Connecting to OSS: {bucket_name} at {endpoint}")
          auth = oss2.Auth(access_key_id, access_key_secret)
          bucket = oss2.Bucket(auth, endpoint, bucket_name)
          
          dist_path = Path('dist')
          if not dist_path.exists():
              print("❌ Error: dist directory not found!")
              exit(1)
          
          print("Uploading files...")
          for file_path in dist_path.rglob('*'):
              if file_path.is_file():
                  object_name = file_path.relative_to(dist_path).as_posix()
                  print(f"Uploading: {object_name}")
                  bucket.put_object_from_file(object_name, str(file_path))
          
          print("✅ Deployment completed successfully!")
          EOF
      
      - name: Success message
        run: |
          echo "🚀 Website deployed successfully to Aliyun OSS!"
          echo "🌐 Access your site at: https://${{ secrets.ALIYUN_OSS_BUCKET }}.oss-cn-beijing.aliyuncs.com"

